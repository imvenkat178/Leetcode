class Solution {
    public int mirrorDistance(int n) {
        String st = new StringBuilder(Integer.toString(n)).reverse().toString();
        return Math.abs(n - Integer.parseInt(st));
        
    }
}
