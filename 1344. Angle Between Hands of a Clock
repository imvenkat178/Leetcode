class Solution {
    public double angleClock(int hour, int minutes) {
        double hd = (minutes/60.0)*30  + ((hour%12) *30);
        double md = (minutes / 60.0) *360;
        return Math.min(Math.abs(md-hd) , 360 - Math.abs(md-hd));
    }
}
