class Solution {
    public int maxIceCream(int[] costs, int coins) {
        int c = 0;
        long s = 0;
        Arrays.sort(costs);
        for(int i : costs){
            s += i;
            if(s<=coins)
            {
                c++;
            }
        }
        return c;
    }
}
